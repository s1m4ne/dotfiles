[[manager.prepend_keymap]]
on   = "<A-y>"
run  = ["plugin copy-file-contents"]
desc = "Copy contents of file"

# --- TEMP: 動作確認用（単押し y で起動） ---
[[manager.prepend_keymap]]
on = "y"
run = ["plugin copy-file-contents"]
desc = "Copy contents of file (TEMP)"
