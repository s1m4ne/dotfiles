if true then return end -- WARN: REMOVE THIS LINE TO ACTIVATE THIS FILE

-- This file runs last in the setup process.
-- It is plain Lua; put anything here that doesn't fit elsewhere.

-- Example: an autocmd you want to run after the colorscheme is set
-- vim.api.nvim_create_autocmd("ColorScheme", {
--   pattern = "*",
--   callback = function()
--     -- final highlight overrides go here
--     -- vim.api.nvim_set_hl(0, "Normal", { bg = "none" })
--   end,
-- })
